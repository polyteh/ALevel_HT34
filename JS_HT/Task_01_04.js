/*04. В переменную day записан текущий день недели. Если это не суббота и не воскресенье, выведите в console.log сообщение о необходимости идти на работу.

Сделать через тернарку и так же через обычный if, else
*/

let days = ['Mond','Tue','Wend','Thu','Fri','Sat','Sun'];
let dayOfWeek='Mond';

for (var curday in days)
{
if(days[curday]==day) console.log("it is day")
}